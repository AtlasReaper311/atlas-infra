"""Importable assurance tooling for Atlas Infra.

The command-line modules also support direct file execution.  Keeping the
directory an explicit package makes the repository-root and module import
paths unambiguous without changing the existing CLI entry points.
"""
