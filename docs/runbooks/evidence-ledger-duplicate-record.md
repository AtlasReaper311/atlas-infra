# Duplicate evidence record

Treat deterministic duplicates as idempotent. Investigate only if distinct events collapse unexpectedly.

No provider mutation, secret retrieval, or raw evidence storage is permitted.
