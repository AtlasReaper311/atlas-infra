# Forbidden evidence field

Remove secret-bearing or raw-payload fields at the producer. The ledger must remain metadata-only.

No provider mutation, secret retrieval, or raw evidence storage is permitted.
