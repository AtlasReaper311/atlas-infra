# Ambiguous runbook match

Review the ranked reasons and choose the most specific service/category entry. Do not execute any command automatically.

The runbook bot remains read-only and performs no provider or repository mutation.
