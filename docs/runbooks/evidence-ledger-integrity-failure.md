# Evidence database integrity failure

Stop ingestion, preserve the database file, run read-only integrity inspection, and restore from an approved backup if one exists.

No provider mutation, secret retrieval, or raw evidence storage is permitted.
