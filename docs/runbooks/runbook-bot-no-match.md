# No runbook match

Confirm the event type, service ID, state, and category. Add a new index entry through review rather than broadening the matcher at runtime.

The runbook bot remains read-only and performs no provider or repository mutation.
