# Evidence retention failure

Review the configured retention period and prune only expired metadata through the ledger CLI.

No provider mutation, secret retrieval, or raw evidence storage is permitted.
